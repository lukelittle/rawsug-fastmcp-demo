# Copy this file to terraform.tfvars and customize

aws_region   = "us-east-1"
project_name = "vinyl-chatbot"
environment  = "demo"

# Optional: Enable Bedrock for enhanced conversational responses
# Requires Bedrock model access in your AWS account
use_bedrock      = false
bedrock_model_id = "anthropic.claude-3-5-haiku-20241022-v1:0"
# bedrock_region = "us-east-1"  # Defaults to aws_region if not specified
